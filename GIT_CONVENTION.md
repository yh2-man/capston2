# 🌿 FLOWER 프로젝트 Git 협업 수칙 (Git Convention)

본 문서는 팀원들 간의 코드 충돌을 방지하고, 안전하게 코드를 합치기 위해 모두가 지켜야 할 **행동 제한 및 작업 수칙**입니다.

---

## 🚫 1. 절대 금지 규칙 (Zero Tolerance)
1. **`main` 브랜치에 직접 Push 절대 금지**
   - 개발 중인 미완성 코드나 에러가 나는 테스트 코드를 `main`에 직통으로 올리는 것을 엄격히 금지합니다.
   - `main` 브랜치는 '누가 언제 다운받아 앱을 실행해도 에러 없이 완벽하게 돌아가는 상태'여야만 합니다.
2. **다른 사람의 브랜치 임의 수정 금지**
   - 내 담당 기능이 아니라면 허락 없이 타인의 브랜치로 넘어가 코드를 수정하거나 강제로 Push하지 않습니다.

---

## 🔄 2. 작업 시작 행동 수칙
작업을 시작할 때는 '무조건' 남들이 고쳐둔 최신 상태를 내 컴퓨터로 업데이트받은 뒤에 시작해야 충돌이 나지 않습니다.
```bash
git checkout main
git pull origin main
```

---

## 🌿 3. 브랜치(Branch) 생성 및 이름 규칙
항상 최신 `main` 상태에서 본인만의 독립된 작업 공간(브랜치)을 새로 생성한 뒤 코드를 작성합니다. 
브랜치 이름은 누가 봐도 뭘 작업하는지 알 수 있게 **`작업유형/기능이름`** 형식으로 작성합니다.

* **feat/** : 새로운 기능 개발 (예: `feat/login`, `feat/chatbot-ui`)
* **fix/** : 고장난 거 수리/버그 수정 (예: `fix/map-crash`)
* **docs/** : 문서나 README.md 등 텍스트만 건드릴 때 (예: `docs/api-specs`)
* **design/** : 레이아웃이나 CSS, UI 수정 (예: `design/button-color`)

**명령어 지정:** `git checkout -b feat/어쩌구`

---

## 💾 4. 커밋(Commit) 작성 규칙
코드 저장을 깃허브에 기록할 때 "수정함", "ㅁㄴㅇㄹ", "진짜최종" 같은 의미 없는 단어는 금지합니다.
* **형식:** `작업유형: 구체적인 작업 내용`
* **예시:** 
  * `feat: 유저 카카오 로그인 연동`
  * `fix: 채팅창에서 스크롤 안 내려가는 버그 수정`

---

## 🚀 5. 내 코드를 깃허브에 합치기 (Pull Request 과정)

보호 구역인 `main`에 내 코드를 합치려면 반드시 아래의 통제 절차를 따릅니다.

1. **내 브랜치 그대로 깃허브에 올리기**
   ```bash
   git push origin 본인브랜치명
   # 예: git push origin feat/login
   ```
2. **Pull Request(PR) 작성**
   - 깃허브 홈페이지에 들어가 **Pull Request** 버튼을 눌러 승인 요청서를 씁니다.
   - 작성 시 "무엇을 개발했는지", "팀원들이 어느 부분을 테스트해봐야 하는지"를 명확히 적습니다.
3. **상호 리뷰 (Code Review)**
   - 내가 PR을 올렸다고 해서 내 맘대로 초록색 버튼(Merge)을 누르고 합치면 안 됩니다.
   - 최소 1명 이상의 다른 팀원이 눈으로 코드를 훑어보고 이상이 없다고 **승인(Approve) 댓글이나 사인**을 줬을 때만 최종적으로 `main`에 합칠(Merge) 수 있습니다.
4. **합친 후 청소**
   - 무사히 `main`에 병합이 끝났다면, 헷갈리지 않게 다 쓴 본인의 브랜치는 삭제합니다.
   - 그리고 다음 새로운 작업을 하려면 **규칙 2번(작업 시작 행동 수칙)**으로 돌아가 다시 최신 코드를 받고 새 브랜치를 파는 행동을 반복합니다.
