# 🌿 팀 깃(Git) 핵심 협업 수칙

팀원 간 코드 충돌과 프로젝트 고장을 방지하기 위한 5가지 필수 규칙입니다!

**1. `main`에는 다이렉트 직행(Push) 금지 🚫**
- 완벽하게 작동하는 `main` 브랜치에는 절대 직접 코드를 올리지 않습니다.

**2. 작업 시작 전 반드시 '최신화' 먼저 🔄**
- 코드 짜기 전 무조건 `git pull origin main`을 쳐서 남들이 짜둔 최신 코드로 빙의(?)한 다음 시작합니다.

**3. 각자의 평행우주(브랜치) 파서 작업 🌿**
- 항상 새 브랜치를 만들어 넘어갑니다. 이름은 `feat/login`, `fix/에러수정` 처럼 '작업종류/기능이름' 형식으로 짓습니다.
- 명령어: `git checkout -b feat/이름`

**4. 헛소리 방지용 커밋(Commit) 메시지 💬**
- 기록을 남길 때 "ㅇㅇ", "최종", "진짜끝" 같은 의미 없는 말은 금지합니다. 
- 예시: `feat: 앱 푸시 알림 기능 추가`

**5. 합치기 전 무조건 사인지 받기 (Pull Request) 🚀**
- 내 완성 코드를 `main`에 섞고 싶을 땐 무조건 **Pull Request**를 보냅니다.
- **다른 팀원이 코드를 쓱 보고 이상이 없다고 승인(Approve)을 눌러줬을 때만** 최종적으로 합치기(Merge)를 할 수 있습니다.
